services:
  rayobrowse:
    image: rayobyte/rayobrowse:latest
    restart: unless-stopped
    shm_size: "2g"
    expose:
      - "9222"
    volumes:
      - rayobrowse-data:/data
    env_file:
      - .env

volumes:
  rayobrowse-data:
